

// const corsOptins = {
//     origin: (origin,callback) =>{
//         if(whiteListedNetworks.indexOf(origin) !== -1 || !origin){
//            return callback(null,true);
//         }
//         return callback(new Error("Origin is not among the whitelisted networks"),false);
//     }
// }

// export default corsOptins;